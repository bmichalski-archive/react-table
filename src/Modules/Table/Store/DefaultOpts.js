import undefinedFunction from '../../../Common/UndefinedFunction'

export default {
  getData: undefinedFunction.bind('table.getData'),
  renderCell: undefinedFunction.bind('table.renderCell'),
  messages: {
    data: {
      errorLoading: 'Error loading data.',
      loadingMessage: 'Loading data...'
    }
  }
}